# Block web access to sqlite db, backups, logs, lock files.
# Apache 2.4 syntax first (live box is Apache/2.4, AI.md), 2.2 fallback guarded.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
